# N1.1 Go-Live Readiness Checklist (v1)

Use this to decide **Ready / Not Ready** for N1.1 pilot release.

## A) Content readiness
- [x] N1.1 diagnostic set finalized (4 items) — see `docs/product/n1.1-item-spec-v2.md`
- [x] N1.1 shadow set finalized (6 items, 2 per route) — see `docs/product/n1.1-item-spec-v2.md`
- [x] Every item has metadata tags (`question_role`, `misconception_tag`, `route`, `transfer_level`, `strictness_level`) — aligned in `docs/product/n1.1-item-spec-v2.md` and `docs/product/seeds/n1.1-seed.sql`
- [x] At least 1 non-MCQ item is included in mastery gate cycle — explicit per-route coverage in `docs/product/n1.1-item-spec-v2.md`

## B) Routing readiness
- [x] Route decision service active (`secure` or `A/B/C`) — secure fast-pass now emits via `explanation_route_assigned` payload
- [x] Route fallback chain works (`A -> B -> C -> intervention`) — covered by `src/__tests__/n11RouteLogic.test.ts`
- [x] Transfer signal included in secure decision — secure fast-pass now requires a positive transfer signal (`question_role=transfer` correct)
- [x] Route recommendation persisted in diagnostic payload — stored in `diagnosticSession.payload.routeRecommendations[skillCode]` via `persistRouteRecommendation(...)`

## C) Input/UX readiness
- [x] MCQ rendering stable — covered by `src/__tests__/learnSession.rendering.test.tsx`
- [x] Short text/short numeric rendering stable — covered by `src/__tests__/learnSession.shortEntryRendering.test.tsx`
- [x] Input normalization works for entry grading — covered by `src/__tests__/gradeAttempt.test.ts` (case/whitespace/punctuation/boolean/diacritics/multi-answer parsing)
- [x] Malformed/missing options fail gracefully (no crash) — safe parser coverage in `src/__tests__/itemMeta.parseOptions.test.ts`; UI fallback message present in Learn/Diagnostic/Baseline clients
- [x] Student always sees clear next step — covered by `src/__tests__/learnSession.nextStep.test.tsx` (intro/session/results CTA path)

## D) Mastery + intervention readiness
- [x] Shadow pair pass/fail events emitted — covered by `src/__tests__/learnAttempt.shadowEvents.test.ts` (pass path emits `shadow_pair_passed`; fail path emits `shadow_pair_failed`)
- [x] Not Yet / Developing / Secure transitions are accurate — covered by `src/__tests__/skillStatus.transitions.test.ts` (+ thresholds validated in `src/__tests__/mastery.test.ts`)
- [x] Route C repeated failure triggers intervention flag — covered by `src/__tests__/routeAssignment.intervention.test.ts` (fallback-chain exhaustion recommends intervention) and `src/__tests__/learnAttempt.shadowEvents.test.ts` (route C failed shadow pair emits `intervention_flagged` + upserts flag)
- [x] No mismatch between UI summary and mastery records — covered by `src/__tests__/learnAttempt.masteryConsistency.test.ts` (final rollup passed to `updateSkillMastery` matches session correct/total summary)

## E) Teacher visibility readiness
- [x] Admin interventions page shows route summary (A/B/C) — covered by `src/__tests__/adminInterventions.summary.test.tsx`
- [x] Shadow pass/fail counts visible — covered by `src/__tests__/adminInterventions.summary.test.tsx`
- [x] Secure fast-pass count visible — covered by `src/__tests__/adminInterventions.summary.test.tsx`
- [x] Intervention flagged count visible — covered by `src/__tests__/adminInterventions.summary.test.tsx`

## F) QA readiness
- [x] Route unit tests pass (N1 routing cases) — `n1Routing.test.ts`, `n11RouteLogic.test.ts`, `routeAssignment.intervention.test.ts`, `routeAssignment.transferSignal.test.ts`
- [x] UI smoke checklist passes (`docs/qa/ui-smoke-checklist-v1.md`) — automated smoke coverage green via `learnSession.rendering.test.tsx`, `learnSession.shortEntryRendering.test.tsx`, `learnSession.nextStep.test.tsx`, `learnAttempt.masteryConsistency.test.ts`
- [x] Persona harness passes (secure / misconception / repeated-fail) — covered by `src/__tests__/n11PersonaHarness.test.ts`
- [x] No stop-ship defects open — full test suite green (`npm test`: 19 files, 96 tests passing)

## G) Runtime readiness
- [ ] Dependency install and runtime stable on target environment — build currently blocked by pre-existing TS typing errors (latest: `src/features/reteach/routeLoader.ts` step interaction typing)
- [x] Seed SQL applied (`docs/product/seeds/n1.1-seed.sql`) — executed via `npx prisma db execute --schema prisma/schema.prisma --file ./docs/product/seeds/n1.1-seed.sql`
- [x] DB sanity checks pass (skill + 10 linked items) — N1.1 found with 20 linked items (>=10)
- [x] Telemetry events observable in logs — event table shows live counts for `attempt_submitted`, `attempt_graded`, `explanation_route_assigned`, `shadow_pair_passed/failed`, `intervention_flagged`

---

## Go / No-Go rule
**GO** when all sections A–G are complete (or one explicitly accepted exception with owner + date).

**NO-GO** if any of these are false:
1. Routing correctness not verified
2. Shadow gate transitions not verified
3. Teacher intervention visibility missing
4. Runtime instability prevents pilot monitoring

---

## Pilot launch note template
- Date:
- Environment:
- Cohort size:
- Owner:
- Exceptions accepted:
- Rollback plan verified: [ ]

This checklist should be updated at each milestone and used as the single source of truth for N1.1 launch readiness.